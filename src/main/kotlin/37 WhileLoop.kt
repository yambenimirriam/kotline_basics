fun main() {
    var number = 1
    while (number <= 5) {
        println("Number $number")
        ++number
    }

    val brand  = arrayOf( "mi", "mi", "e")
    var index = 0
    while (index <= brand.size - 1) {
        println(brand [index])
        ++index
    }
}