fun main() {
    val isAdult = false
    val isFemale = true

    println(isAdult)
    println(isFemale)

    val orderCompleted: Boolean? = null
    println(orderCompleted)
    // naming conversion for boolean(mostly ask question whe dealing with booleans
}