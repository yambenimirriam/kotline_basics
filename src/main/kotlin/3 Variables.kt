fun main() {
    var name: String = "Mimie"
    var age: Int = 11

    println(name)
    println(age)
    // to combine this we use a dollar sign

    println("${name} is ${age} years old.")
    println("Name: ${name} Age: ${age} ")

}